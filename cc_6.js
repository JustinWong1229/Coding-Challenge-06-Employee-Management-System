class Employee {
    constructor (name, deparment) {
        this.name = name
        this.deparment = deparment
    }
    describe () {
        `${this.name} works in the ${this.deparment}.`
    }
} 
